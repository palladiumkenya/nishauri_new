import 'package:flutter/material.dart';

class EmergencyContactInformation extends StatelessWidget {
  const EmergencyContactInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
