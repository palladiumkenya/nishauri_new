import 'package:flutter/material.dart';

class MedicalHistoryInformation extends StatelessWidget {
  const MedicalHistoryInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
