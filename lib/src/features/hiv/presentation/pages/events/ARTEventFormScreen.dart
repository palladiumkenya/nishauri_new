import 'package:flutter/material.dart';

class ARTEventFormScreen extends StatelessWidget {
  const ARTEventFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
