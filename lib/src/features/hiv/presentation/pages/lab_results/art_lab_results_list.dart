import 'package:flutter/material.dart';

class ARTLabResultsList extends StatelessWidget {
  const ARTLabResultsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
