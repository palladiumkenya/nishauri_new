import 'package:flutter/material.dart';

class ARTLabResultTrend extends StatelessWidget {
  const ARTLabResultTrend({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
