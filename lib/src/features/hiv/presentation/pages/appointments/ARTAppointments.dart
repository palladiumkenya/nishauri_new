import 'package:flutter/material.dart';

class ARTAppointmentsScreen extends StatelessWidget {
  const ARTAppointmentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
